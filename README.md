# portkill-shell
## How to use
1. move root path
2. open `~/.bashrc` or `~/.zshrc` in vim, vsocde ...
3. add this function

or execute this sh file.
Then you can use this command
```
portkill [your port]
```
